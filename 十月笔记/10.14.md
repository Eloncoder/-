# runtime

```
#include<>

#define USE_STRUCTURE_SETUP
get_memory_data();
get_input_array();
init_global_var();
init_global_var_with_arguments();

// pcie
#ifdef USE_PCIE
// PCIE by xyy
g_pPcReadyOffset = 0xed00000;
g_
g_



#endif
// USE_PCIE
// pcie

int main(void){

  // enable cache
  Xil_
  Xil_
  
  
  //pcie
#ifdef USE_PCIE
  MALLOC_
  MALLOC_
  MALLOC_
 
  // clear all const address
  memset(
  memset(
  memset(
  memset(
#endif // USE_PCIE
  // pcie
  
  
  
  // extern memory
  
  // malloc for global memory pool
  
  // inference setup for all cores
  for  CORE_NUM



#ifndef USE_PCIE


#ifndef CNT_RUN_TIME
  // count cycles
#endif  // CNT_RUN_TIME

  // do inference
  
#ifndef CNT_RUN_TIME
  // calculate cycles
#endif  // CNT_RUN_TIME

  // disable cache
  // top-5
  // 
  // top-5
  
  
#else  // USE_PCIE
  // pcie
  // received start inference cmd, send feaeture info to pc
  //init network input memory
  

}
```
